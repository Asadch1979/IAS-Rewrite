@{
    ViewData["Title"] = "ObservationAuditTrail";
    Layout = "_Layout";
}
<h4>ObservationAuditTrail</h4>
<p>Shows the full action log (audit trail) for any observation (who did what, when, at each stage).</p>
